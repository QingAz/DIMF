base_config: configs/multistage_aligned.yaml
run_name: multistage_aligned_sweep
selection_metric: best_val
selection_mode: min
sweep_seed: 42

search:
  mode: random
  random_seed: 20260329
  n_trials: 12
  parameters:
    train.batch_size: [32, 64, 128]
    train.lr: [0.0001, 0.001, 0.01]
    model.hidden_dim: [48, 64, 96]
    model.dropout: [0.05, 0.1, 0.2]
